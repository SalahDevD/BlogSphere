{% extends 'base.html.twig' %}

{% block title %}Dashboard Admin - BlogSphere{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/admin.css') }}">
{% endblock %}

{% block body %}
<div class="container admin-container">
    <div class="admin-header">
        <div>
            <h1>🛡️ Dashboard Admin</h1>
            <p>Gérez l'ensemble de la plateforme BlogSphere</p>
        </div>
        <div class="admin-nav">
            <a href="{{ path('app_home') }}" class="btn">← Retour au site</a>
        </div>
    </div>

    <div class="stats-grid">
        <div class="stat-card">
            <div class="stat-icon">📝</div>
            <div class="stat-value">{{ totalArticles }}</div>
            <div class="stat-label">Articles total</div>
        </div>
        <div class="stat-card">
            <div class="stat-icon">✅</div>
            <div class="stat-value">{{ publishedArticles }}</div>
            <div class="stat-label">Articles publiés</div>
        </div>
        <div class="stat-card">
            <div class="stat-icon">📄</div>
            <div class="stat-value">{{ draftArticles }}</div>
            <div class="stat-label">Brouillons</div>
        </div>
        <div class="stat-card">
            <div class="stat-icon">👥</div>
            <div class="stat-value">{{ totalUsers }}</div>
            <div class="stat-label">Utilisateurs</div>
        </div>
        <div class="stat-card">
            <div class="stat-icon">💬</div>
            <div class="stat-value">{{ pendingComments }}</div>
            <div class="stat-label">Commentaires en attente</div>
        </div>
    </div>

    <div class="info-section">
        <h2 class="section-title">⚡ Actions rapides</h2>
        <div class="info-grid">
            <div class="info-item">
                <strong>Gestion des utilisateurs</strong>
                <p><a href="{{ path('admin_users_index') }}">Voir tous les utilisateurs →</a></p>
            </div>
            <div class="info-item">
                <strong>Modération</strong>
                <p><a href="{{ path('supervisor_moderation_dashboard') }}">Aller à la modération →</a></p>
            </div>
            <div class="info-item">
                <strong>Statistiques</strong>
                <p><a href="{{ path('admin_stats_dashboard') }}">Voir les statistiques →</a></p>
            </div>
        </div>
    </div>

    <div class="info-section">
        <h2 class="section-title">ℹ️ Informations système</h2>
        <div class="info-grid">
            <div class="info-item">
                <strong>Total articles</strong>
                <p>{{ totalArticles }}</p>
            </div>
            <div class="info-item">
                <strong>Articles publiés</strong>
                <p>{{ publishedArticles }}</p>
            </div>
            <div class="info-item">
                <strong>Brouillons</strong>
                <p>{{ draftArticles }}</p>
            </div>
            <div class="info-item">
                <strong>Total utilisateurs</strong>
                <p>{{ totalUsers }}</p>
            </div>
            <div class="info-item">
                <strong>Commentaires en attente</strong>
                <p>{{ pendingComments }}</p>
            </div>
        </div>
    </div>
</div>
{% endblock %}
