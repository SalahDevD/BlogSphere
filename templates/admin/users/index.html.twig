{% extends 'base.html.twig' %}

{% block title %}Gestion des utilisateurs - Admin - BlogSphere{% endblock %}

{% block stylesheets %}
<style>
    :root {
        --orange-primary: #FF6B35;
        --orange-dark: #E85A2A;
        --gray-900: #111827;
        --gray-700: #374151;
        --gray-600: #4b5563;
        --gray-200: #e5e7eb;
        --gray-50: #f9fafb;
        --white: #ffffff;
    }

    .users-container {
        background: var(--gray-50);
        min-height: 100vh;
        padding: 4rem 0;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 2rem;
    }

    .header {
        margin-bottom: 3rem;
    }

    .header h1 {
        font-size: 2.5rem;
        font-weight: 900;
        color: var(--gray-900);
        margin-bottom: 0.5rem;
    }

    .search-filters {
        background: var(--white);
        padding: 1.5rem;
        border-radius: 12px;
        margin-bottom: 2rem;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .search-filters input,
    .search-filters select {
        padding: 0.75rem 1rem;
        border: 2px solid var(--gray-200);
        border-radius: 8px;
        font-family: inherit;
        font-size: 1rem;
    }

    .search-filters input:focus,
    .search-filters select:focus {
        outline: none;
        border-color: var(--orange-primary);
    }

    .search-filters input {
        flex: 1;
        min-width: 250px;
    }

    .search-filters button {
        padding: 0.75rem 1.5rem;
        background: linear-gradient(135deg, var(--orange-primary), var(--orange-dark));
        color: var(--white);
        border: none;
        border-radius: 8px;
        cursor: pointer;
        font-weight: 600;
        transition: all 0.3s;
    }

    .search-filters button:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(255, 107, 53, 0.3);
    }

    .table-wrapper {
        background: var(--white);
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    }

    table {
        width: 100%;
        border-collapse: collapse;
    }

    thead {
        background: var(--gray-50);
        border-bottom: 2px solid var(--gray-200);
    }

    th {
        padding: 1rem;
        text-align: left;
        font-weight: 700;
        color: var(--gray-900);
    }

    td {
        padding: 1rem;
        border-bottom: 1px solid var(--gray-200);
        color: var(--gray-700);
    }

    tbody tr:hover {
        background: var(--gray-50);
    }

    .role-badge {
        display: inline-block;
        padding: 0.35rem 0.85rem;
        border-radius: 20px;
        font-size: 0.75rem;
        font-weight: 700;
        text-transform: uppercase;
        white-space: nowrap;
    }

    .role-admin {
        background: #fee2e2;
        color: #991b1b;
    }

    .role-supervisor {
        background: #dbeafe;
        color: #1e40af;
    }

    .role-editor {
        background: #f0fdf4;
        color: #15803d;
    }

    .role-user {
        background: #fef3c7;
        color: #92400e;
    }

    .actions {
        display: flex;
        gap: 0.5rem;
    }

    .btn {
        padding: 0.4rem 0.8rem;
        border: none;
        border-radius: 6px;
        cursor: pointer;
        font-weight: 600;
        font-size: 0.85rem;
        text-decoration: none;
        transition: all 0.2s;
        display: inline-block;
    }

    .btn-edit {
        background: #3b82f6;
        color: var(--white);
    }

    .btn-edit:hover {
        background: #2563eb;
    }

    .btn-delete {
        background: #ef4444;
        color: var(--white);
    }

    .btn-delete:hover {
        background: #dc2626;
    }

    .empty-state {
        text-align: center;
        padding: 4rem 2rem;
    }

    .empty-state-emoji {
        font-size: 4rem;
        margin-bottom: 1rem;
    }

    .empty-state h2 {
        font-size: 1.5rem;
        color: var(--gray-900);
        margin-bottom: 0.5rem;
    }

    @media (max-width: 768px) {
        .search-filters {
            flex-direction: column;
        }

        .search-filters input {
            min-width: 100%;
        }

        table {
            font-size: 0.9rem;
        }

        th, td {
            padding: 0.75rem;
        }

        .container {
            padding: 0 1rem;
        }
    }
</style>
{% endblock %}

{% block body %}
<div class="users-container">
    <div class="container">
        <div class="header">
            <h1>👥 Gestion des utilisateurs</h1>
            <p style="color: var(--gray-600);">Affichage de {{ users|length }} utilisateur(s)</p>
            <a href="{{ path('admin_users_new') }}" style="display: inline-block; margin-top: 1rem; padding: 0.75rem 1.5rem; background: linear-gradient(135deg, #FF6B35, #E85A2A); color: white; border: none; border-radius: 8px; cursor: pointer; font-weight: 600; text-decoration: none; transition: all 0.3s;">
                ➕ Ajouter un utilisateur
            </a>
        </div>

        <div class="search-filters">
            <form method="GET" style="display: flex; gap: 1rem; flex: 1; flex-wrap: wrap; align-items: center;">
                <input type="text" name="search" placeholder="Rechercher par nom ou email..." value="{{ search ?? '' }}" style="flex: 1; min-width: 250px;">
                <select name="role" style="padding: 0.75rem 1rem; border: 2px solid #e5e7eb; border-radius: 8px; font-family: inherit; font-size: 1rem; cursor: pointer;">
                    <option value="">Tous les rôles</option>
                    <option value="ROLE_USER" {% if (selectedRole ?? '') == 'ROLE_USER' %}selected{% endif %}>👤 Utilisateur</option>
                    <option value="ROLE_EDITOR" {% if (selectedRole ?? '') == 'ROLE_EDITOR' %}selected{% endif %}>✏️ Éditeur</option>
                    <option value="ROLE_SUPERVISOR" {% if (selectedRole ?? '') == 'ROLE_SUPERVISOR' %}selected{% endif %}>👀 Superviseur</option>
                    <option value="ROLE_ADMIN" {% if (selectedRole ?? '') == 'ROLE_ADMIN' %}selected{% endif %}>🔐 Administrateur</option>
                </select>
                <button type="submit" style="padding: 0.75rem 1.5rem; background: #FF6B35; color: white; border: none; border-radius: 8px; cursor: pointer; font-weight: 600; transition: all 0.3s; white-space: nowrap;">🔍 Filtrer</button>
                {% if (search ?? '') or (selectedRole ?? '') %}
                    <a href="{{ path('admin_users_index') }}" style="padding: 0.75rem 1.5rem; background: #e5e7eb; color: #374151; border: none; border-radius: 8px; cursor: pointer; font-weight: 600; transition: all 0.3s; text-decoration: none; white-space: nowrap;">❌ Réinitialiser</a>
                {% endif %}
            </form>
        </div>

        {# Flash Messages #}
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div style="padding: 1rem 1.5rem; background: {{ label == 'error' ? '#fee2e2' : '#d4edda' }}; color: {{ label == 'error' ? '#991b1b' : '#155724' }}; border: 1px solid {{ label == 'error' ? '#f5c6cb' : '#c3e6cb' }}; border-radius: 8px; margin-bottom: 1.5rem;">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}

        {% if users|length > 0 %}
            <div class="table-wrapper">
                <table>
                    <thead>
                        <tr>
                            <th>Nom</th>
                            <th>Email</th>
                            <th>Rôles</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for user in users %}
                            <tr>
                                <td>
                                    <strong>{{ user.name }}</strong>
                                </td>
                                <td>{{ user.email }}</td>
                                <td>
                                    {% set roleLabels = {
                                        'ROLE_USER': '👤 Utilisateur',
                                        'ROLE_EDITOR': '✏️ Éditeur',
                                        'ROLE_SUPERVISOR': '👀 Superviseur',
                                        'ROLE_ADMIN': '🔐 Administrateur'
                                    } %}
                                    {% for role in user.roles %}
                                        {% set roleDisplay = role|replace({'ROLE_': ''}) %}
                                        <span class="role-badge role-{{ roleDisplay|lower }}" style="display: inline-block; margin-right: 0.5rem; margin-bottom: 0.5rem;">
                                            {{ roleLabels[role] ?? roleDisplay }}
                                        </span>
                                    {% endfor %}
                                </td>
                                <td>
                                    <div class="actions">
                                        <a href="{{ path('admin_users_edit', {id: user.id}) }}" class="btn btn-edit">Modifier</a>
                                        <form method="POST" action="{{ path('admin_users_delete', {id: user.id}) }}" style="display: inline;" onsubmit="return confirm('Êtes-vous sûr ?');">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
                                            <button type="submit" class="btn btn-delete">Supprimer</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        {% else %}
            <div class="table-wrapper">
                <div class="empty-state">
                    <div class="empty-state-emoji">👤</div>
                    <h2>Aucun utilisateur trouvé</h2>
                    <p style="color: var(--gray-600);">Modifiez vos filtres pour voir les utilisateurs</p>
                </div>
            </div>
        {% endif %}
    </div>
</div>
{% endblock %}
