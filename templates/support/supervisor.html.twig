{% extends 'base.html.twig' %}

{% block title %}Gestion Support - BlogSphere{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/support.css') }}">
{% endblock %}

{% block body %}
<div class="support-supervisor-container">
    <div class="page-header">
        <h1 class="page-title">💬 Gestion du Support</h1>
        <p class="page-subtitle">Gérez toutes les conversations avec les utilisateurs</p>
    </div>

    {% if conversations|length > 0 %}
        {% for conversation in conversations %}
            <div class="conversation-card">
                <div class="conversation-header">
                    <div class="user-info">
                        <div class="user-avatar">
                            {{ conversation.user.name|first|upper }}
                        </div>
                        <span class="user-name">{{ conversation.user.name }}</span>
                    </div>
                    {% if conversation.unread > 0 %}
                        <span class="unread-badge">
                            🔔 {{ conversation.unread }} nouveau{% if conversation.unread > 1 %}x{% endif %}
                        </span>
                    {% endif %}
                </div>

                <div class="messages-container">
                    {% for message in conversation.messages %}
                        <div class="message-item {% if message.sender == conversation.user %}message-from-user{% else %}message-from-supervisor{% endif %}">
                            <div class="message-sender">
                                {% if message.sender == conversation.user %}
                                    👤 {{ message.sender.name }}
                                {% else %}
                                    🛡️ {{ message.sender.name }} (Superviseur)
                                {% endif %}
                            </div>
                            <div class="message-content">
                                {{ message.content }}
                            </div>
                            <div class="message-time">
                                📅 {{ message.createdAt|date('d/m/Y H:i') }}
                            </div>
                        </div>
                    {% endfor %}
                </div>

                <div class="reply-form">
                    <form method="POST" action="{{ path('support_supervisor_reply', {userId: conversation.user.id}) }}">
                        <textarea name="message" placeholder="Tapez votre réponse ici..." required></textarea>
                        <button type="submit">📤 Envoyer la réponse</button>
                    </form>
                </div>
            </div>
        {% endfor %}
    {% else %}
        <div class="empty-state">
            <div class="empty-state-icon">💬</div>
            <div class="empty-state-text">
                Aucune conversation de support pour le moment.
            </div>
            <p style="margin-top: 1rem; color: var(--gray-400);">
                Les utilisateurs pourront vous contacter via le bouton de support.
            </p>
        </div>
    {% endif %}
</div>
{% endblock %}
