{% extends 'base.html.twig' %}

{% block title %}Messages Support - Supervision{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/supervisor.css') }}">
{% endblock %}

{% block body %}
<div class="messages-container">
    <div class="messages-header">
        <h1>📨 Messages de Support</h1>
        <p>Gérez les messages de support des clients</p>
    </div>

    {% if messages|length > 0 %}
        <div class="messages-table">
            <div class="messages-table-header">
                <div>Sujet</div>
                <div>Utilisateur</div>
                <div>Type</div>
                <div>Créé le</div>
                <div>Actions</div>
            </div>
            <div class="messages-table-body">
                {% for message in messages %}
                    {% if message.parentMessage is null %}
                        <div class="message-row">
                            <div>
                                <div class="message-subject">{{ message.subject }}</div>
                                <div class="message-sender">{{ message.sender.firstName }} {{ message.sender.lastName }}</div>
                            </div>
                            <div>{{ message.sender.email }}</div>
                            <div>
                                <span class="message-type type-{{ message.messageType }}">
                                    {% if message.messageType == 'question' %}
                                        ❓ Question
                                    {% elseif message.messageType == 'problem' %}
                                        ⚠️ Problème
                                    {% else %}
                                        📌 Autre
                                    {% endif %}
                                </span>
                            </div>
                            <div class="message-date">{{ message.createdAt|date('d/m/Y H:i') }}</div>
                            <div class="message-actions">
                                <a href="{{ path('app_contact_support_conversation', {id: message.id}) }}" class="btn-small btn-view">Voir</a>
                            </div>
                        </div>
                    {% endif %}
                {% endfor %}
            </div>
        </div>
    {% else %}
        <div class="empty-state">
            <p>Aucun message de support pour le moment.</p>
        </div>
    {% endif %}
</div>
{% endblock %}
