{% extends 'base.html.twig' %}

{% block title %}Mon Profil - BlogSphere{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/profile.css') }}">
{% endblock %}

{% block body %}
<div class="profile-container">
    <div class="profile-header">
        <div class="profile-info">
            <div class="profile-avatar">
                {% set profileImage = user.images|filter(img => img.isProfile)|first %}
                {% if profileImage %}
                    <img src="{{ asset('uploads/profile/' ~ profileImage.filename) }}" alt="Profile">
                {% else %}
                    {{ user.name|first|upper }}
                {% endif %}
            </div>
            <div class="profile-details">
                <h1 class="profile-name">{{ user.name }}</h1>
                <p class="profile-email">📧 {{ user.email }}</p>
                <a href="{{ path('user_profile_edit') }}" class="btn btn-outline">✏️ Modifier mon profil</a>
                {% if user.bio %}
                    <p class="profile-bio">{{ user.bio }}</p>
                {% endif %}
            </div>
        </div>

        <div class="profile-stats">
            <div class="stat-item">
                <div class="stat-value">{{ articles|length }}</div>
                <div class="stat-label">Articles total</div>
            </div>
            <div class="stat-item">
                <div class="stat-value">
                    {% set approved_count = 0 %}
                    {% for article in articles %}
                        {% if article.validationStatus == 'approved' %}
                            {% set approved_count = approved_count + 1 %}
                        {% endif %}
                    {% endfor %}
                    {{ approved_count }}
                </div>
                <div class="stat-label">Articles validés</div>
            </div>
            <div class="stat-item">
                <div class="stat-value">
                    {% set pending_count = 0 %}
                    {% for article in articles %}
                        {% if article.validationStatus == 'pending' %}
                            {% set pending_count = pending_count + 1 %}
                        {% endif %}
                    {% endfor %}
                    {{ pending_count }}
                </div>
                <div class="stat-label">En attente</div>
            </div>
        </div>
    </div>

    <div class="articles-section">
        <div class="section-header">
            <h2 class="section-title">📚 Mes Articles</h2>
            {% if hasApprovedArticle %}
                <a href="{{ path('app_article_new') }}" class="btn">➕ Nouvel article</a>
            {% endif %}
        </div>

        {% if articles|length > 0 %}
            <div class="articles-grid">
                {% for article in articles %}
                    <div class="article-card">
                        {% if article.image %}
                            <img src="{{ asset('uploads/articles/' ~ article.image) }}" alt="{{ article.title }}" class="article-card-image">
                        {% else %}
                            <div class="article-card-image" style="background: linear-gradient(135deg, var(--orange-primary), var(--orange-dark)); display: flex; align-items: center; justify-content: center; color: white; font-size: 3rem;">
                                📄
                            </div>
                        {% endif %}

                        <div class="article-card-content">
                            <h3 class="article-card-title">{{ article.title }}</h3>
                            
                            <p class="article-card-excerpt">
                                {{ article.content|slice(0, 120) }}...
                            </p>

                            <div class="article-card-meta">
                                <span>📅 {{ article.createdAt|date('d/m/Y') }}</span>
                                <span>
                                    {% if article.validationStatus == 'approved' %}
                                        <span class="status-badge status-approved">✅ Validé</span>
                                    {% elseif article.validationStatus == 'pending' %}
                                        <span class="status-badge status-pending">⏳ En attente</span>
                                    {% else %}
                                        <span class="status-badge status-rejected">❌ Rejeté</span>
                                    {% endif %}
                                </span>
                            </div>

                            <div class="article-card-actions">
                                <a href="{{ path('app_article_show', {id: article.id}) }}" class="btn-small btn-view">
                                    👁️ Voir
                                </a>
                                <a href="{{ path('app_article_edit', {id: article.id}) }}" class="btn-small btn-edit">
                                    ✏️ Modifier
                                </a>
                                <form method="POST" action="{{ path('app_article_delete', {id: article.id}) }}" style="display: inline;">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}">
                                    <button type="submit" class="btn-small btn-delete" onclick="return confirm('Supprimer cet article ?')">
                                        🗑️ Supprimer
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% else %}
            <div class="empty-state">
                <div class="empty-state-icon">📝</div>
                {% if hasApprovedArticle %}
                    <p style="font-size: 1.125rem; margin-bottom: 1rem;">Créez un nouvel article</p>
                    <a href="{{ path('app_article_new') }}" class="btn">➕ Nouvel article</a>
                {% else %}
                    <p style="font-size: 1.125rem; margin-bottom: 1rem;">Vous n'avez pas encore d'articles</p>
                    <a href="{{ path('app_article_new') }}" class="btn">Créer mon premier article</a>
                    <p style="font-size: 0.9rem; color: #6b5563; margin-top: 1rem;">Votre premier article sera soumis à validation par un superviseur. Une fois approuvé, vous pourrez publier librement vos prochains articles.</p>
                {% endif %}
            </div>
        {% endif %}
    </div>
</div>
{% endblock %}
